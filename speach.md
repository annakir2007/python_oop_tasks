Отлично. Ниже — **полный, связный спич**, который можно буквально читать на паре.
Без воды, с понятными формулировками, с примерами и логикой от простого к архитектуре.

---

# Практическое занятие

## Архитектура ЛР-1 и планирование неймспейсов в Python

---

## Часть 1. Разминка — что такое неймспейс

Ребята, сегодня мы не пишем код.
Сегодня мы учимся **думать до кода**.

Начнём с ключевого понятия — **неймспейс**.

### Формулировка

**Неймспейс — это пространство имён, в котором хранятся идентификаторы и объекты, связанные с этими именами.**

Проще:

> Неймспейс — это способ не перепутать одно с другим.

Если у нас есть переменная `price` в одном месте и `price` в другом, Python должен понимать — это одно и то же или нет?

Вот для этого и существуют неймспейсы.

---

## Три уровня неймспейсов в Python

Нарисуйте на доске три уровня.

### 1️⃣ Module — файл `.py`

Каждый файл Python — это отдельный неймспейс.

Если у вас есть:

```
model.py
demo.py
```

Переменная `x` в `model.py` — это не та же самая `x`, что в `demo.py`.

Это два разных пространства имён.

> Каждый файл — это отдельный словарь имён.

---

### 2️⃣ Package — папка

Если у нас есть структура:

```
src/
  lab01/
    model.py
    demo.py
```

Папка `lab01` — это уже уровень выше.
Она объединяет модули в общий логический неймспейс.

Когда вы пишете:

```python
from lab01.model import Student
```

Вы явно указываете, из какого пространства берёте объект.

---

### 3️⃣ Object — экземпляр класса

У каждого объекта есть свои атрибуты.

```
s1 = Student(...)
s2 = Student(...)
```

`s1._gpa` и `s2._gpa` — это разные данные.

Каждый объект — это свой маленький неймспейс.

---

### Главная идея

Неймспейс — это инструмент управления сложностью.

Он нужен для того, чтобы:

* не смешивать разные уровни логики,
* не создавать конфликты имён,
* разделять ответственность.

---

# Часть 2. Что такое архитектура даже в маленьком проекте

Сейчас у вас ЛР-1.
Один класс. Кажется — зачем архитектура?

Но проблема не в размере кода.
Проблема в том, **как он устроен**.

---

## Представьте два варианта

### Вариант А

Один файл:

```
lab01.py
```

Внутри:

* класс
* проверки
* вывод
* тесты
* запуск
* 200 строк всего подряд

Это работает.
Но это плохо масштабируется.

---

### Вариант Б

```
model.py
demo.py
```

`model.py` — только логика класса
`demo.py` — только сценарии использования

Это уже архитектура.

---

## Формулировка

**Архитектура — это разделение ответственности между частями системы.**

Не “много файлов”, а именно:

> Кто за что отвечает?

---

# Часть 3. Как проектировать ЛР-1 до написания кода

Теперь переходим к самому важному.

Вы выбираете предметную область.

Например:

* Банк
* Магазин
* Библиотека
* Игра
* Медицина

Что делают слабые студенты?

Они сразу открывают VS Code и начинают писать код.

Что делают сильные?

Они сначала задают 5 вопросов.

---

## Вопрос 1. Что является сущностью?

Не “что мне хочется написать”, а:

> Какой объект реально существует в этой предметной области?

Примеры:

* BankAccount
* Product
* Patient
* Car
* Player

---

## Вопрос 2. Какие у него атрибуты?

Минимум 4.

Но не просто “что-нибудь”.

Каждый атрибут должен отвечать на вопрос:

> Это характеристика объекта или действие?

Например:

У банковского счёта:

* owner
* balance
* currency
* is_active

Это характеристики.

А вот `withdraw` — это уже действие.

---

## Вопрос 3. Какие инварианты?

Инвариант — это правило, которое всегда должно быть верным.

Например:

* balance ≥ 0
* gpa от 0 до 5
* price ≥ 0
* возраст ≥ 0
* количество ≥ 0

Инвариант — это основа архитектуры.

Плохая архитектура — это когда инварианты не контролируются.

---

## Вопрос 4. Что значит “равенство”?

Очень важный вопрос.

Когда два объекта считаются равными?

Пример:

Два студента равны если совпадает:

* ФИО
* группа

А GPA может отличаться.

У банковского счёта:

* номер счёта — это уникальный идентификатор.

Вот это архитектурное решение.

---

## Вопрос 5. Есть ли состояние?

Это уже уровень “5”.

Есть ли у объекта состояние?

Пример:

У заказа:

* draft
* paid
* shipped
* closed

И правила:

* нельзя “закрыть” если не “оплачен”
* нельзя “оплатить” если уже “закрыт”

Это уже логика поведения.

---

# Часть 4. Архитектура слоёв

Даже в ЛР-1 есть слои.

Нарисуйте пирог.

### Слой 1 — Domain (модель)

Это класс.

Он:

* хранит данные
* проверяет инварианты
* реализует бизнес-методы

Он ничего не знает о том, как его печатают в консоли.

---

### Слой 2 — Application (сценарии)

Это `demo.py`.

Он:

* создаёт объекты
* вызывает методы
* показывает примеры
* ловит ошибки

Он не должен содержать бизнес-логики.

---

### Главное правило

`model.py` не должен импортировать `demo.py`.

Никогда.

Логика не должна зависеть от сценария.

---

# Часть 5. Как выглядит архитектура уровней 3 / 4 / 5

---

## 🟢 Уровень 3

```
model.py
demo.py
```

Валидация прямо в конструкторе.

Минимум магических методов.

Работает — и хорошо.

---

## 🟡 Уровень 4

Добавляется:

* `__repr__`
* сеттер с проверкой
* атрибут класса
* второй бизнес-метод

Архитектурно всё ещё 2 файла.

Но код уже аккуратнее.

---

## 🔴 Уровень 5 — существенная разница

Теперь начинается инженерия.

Добавляем:

```
model.py
validators.py
demo.py
```

Почему?

Потому что:

> Валидация — это отдельная ответственность.

Если вы проверяете:

* имя
* баланс
* цену
* возраст

И делаете это в 3 местах — это плохая архитектура.

Если вы выносите это в отдельные функции — это лучше.

---

## Пример архитектурного улучшения (словами, без кода)

Плохо:

В `__init__` и в сеттере вы пишете:

```
if value < 0:
    raise ValueError
```

Хорошо:

Есть отдельная функция:

```
validate_positive(value)
```

И она вызывается везде.

Это не “больше кода”.
Это “меньше дублирования”.

---

# Часть 6. Планирование неймспейсов правильно

Сейчас очень важная мысль.

Неймспейс — это граница ответственности.

Если файл отвечает за одно — не кладите туда другое.

---

## Правило 1

Один модуль — одна ответственность.

---

## Правило 2

Импорты направлены в одну сторону:

`demo → model → validators`

Никогда наоборот.

---

## Правило 3

Объект сам отвечает за свою корректность.

Не `demo.py` проверяет баланс.

А `BankAccount` сам не даёт стать некорректным.

---

# Часть 7. Как вы должны думать перед написанием кода

Перед тем как открыть VS Code:

1. Нарисовать класс.
2. Выписать атрибуты.
3. Выписать инварианты.
4. Определить равенство.
5. Определить бизнес-методы.
6. Решить — нужны ли состояния.
7. Нарисовать структуру папок.

Только потом писать код.

---

# Финальная мысль

Программирование — это не “написать 200 строк”.

Программирование — это:

> Спроектировать систему так, чтобы она не развалилась через неделю.

Если вы научитесь думать архитектурно даже на ЛР-1 —
дальше вам будет значительно легче.


-------

> **«Превращаем вариант в требования»**
> (10–15 минут живого разбора с доской)

Текст написан так, чтобы вы могли его буквально проговорить.

---

# Часть занятия: Превращаем “вариант” в требования

---

Ребята, сейчас будет очень важный момент.

Мы берём любой вариант из списка —
банк, магазин, медицина, транспорт, игра — неважно.

И наша задача — не написать код.

Наша задача — за 5–7 минут превратить абстрактную идею в чёткие требования к классу.

---

## Шаг 1. Самая частая ошибка

Самая распространённая ошибка — сразу открыть редактор и начать писать:

```python
class BankAccount:
    ...
```

Это ошибка.

Потому что вы ещё не знаете:

* что хранит объект,
* какие у него ограничения,
* какие действия он поддерживает,
* когда он считается “тем же самым” объектом.

Если вы не продумали это — код получится случайным.

---

# Шаг 2. Метод “1 класс за 3 минуты”

Я сейчас дам вам простой шаблон.

Нарисуйте на доске прямоугольник — это наш класс.

И мы последовательно отвечаем на 6 вопросов.

---

## Вопрос 1. Имя класса

Мы должны назвать сущность.

Важно:

> Класс — это существительное.

Не действие.

Не “calculateBalance”.

А:

* BankAccount
* Product
* Patient
* Order
* Player

Имя класса — это тип объекта.

---

## Вопрос 2. Минимум 4 атрибута

Теперь ключевой момент.

Мы задаём себе вопрос:

> Что характеризует этот объект?

Например, если это BankAccount:

* owner
* balance
* currency
* is_active

Если это Product:

* name
* price
* quantity
* category

Если это Patient:

* full_name
* age
* diagnosis
* is_admitted

Обратите внимание:

Атрибут — это характеристика.
Не действие.

Метод — это действие.

---

# Теперь важное правило

Каждый атрибут должен иметь:

* тип
* ограничения
* место проверки

И вот тут мы рисуем таблицу.

---

# Рисунок на доске: Таблица проектирования

| Поле     | Тип   | Ограничение      | Где проверяем |
| -------- | ----- | ---------------- | ------------- |
| price    | float | ≥ 0              | setter        |
| quantity | int   | ≥ 0              | setter        |
| name     | str   | не пустая строка | **init**      |
| category | str   | не пустая строка | **init**      |

---

### Объяснение, которое нужно проговорить

Обратите внимание.

Мы сейчас ещё не написали ни одной строки кода.

Но мы уже:

* понимаем структуру объекта,
* понимаем инварианты,
* понимаем архитектуру проверок.

Это и есть проектирование.

---

# Шаг 3. Инварианты

Теперь вводим термин.

## Формулировка:

**Инвариант — это условие, которое всегда должно оставаться истинным для корректного объекта.**

Примеры:

* Баланс не может быть отрицательным.
* Цена не может быть отрицательной.
* GPA должен быть от 0 до 5.
* Возраст не может быть меньше 0.
* Номер заказа не может быть пустым.

Если инвариант нарушен — объект некорректен.

---

### Очень важная мысль

Кто отвечает за соблюдение инвариантов?

Не demo.py.
Не пользователь.
Не преподаватель.

Сам объект.

Если объект допускает некорректное состояние — архитектура плохая.

---

# Шаг 4. Бизнес-методы

Теперь спрашиваем:

> Что объект умеет делать?

Важно:

Методы — это поведение.

Если это BankAccount:

* deposit(amount)
* withdraw(amount)

Если это Product:

* restock(amount)
* sell(amount)

Если это Patient:

* admit()
* discharge()

Методы должны быть логичными для предметной области.

---

### Здесь нужно проговорить ключевую вещь

Метод — это не просто изменение поля.

Метод должен:

* проверять инварианты,
* менять состояние,
* соблюдать правила предметной области.

Например:

`withdraw(amount)` должен:

* проверить, что amount > 0
* проверить, что amount ≤ balance
* изменить balance

Если этого нет — это не бизнес-логика, это просто изменение переменной.

---

# Шаг 5. Что значит “равенство”?

Очень важный момент.

Когда мы пишем `__eq__`, мы отвечаем на вопрос:

> Когда два объекта считаются одинаковыми?

Это архитектурное решение.

Примеры:

* Два студента равны, если совпадает ФИО и группа.
* Два банковских счёта равны, если совпадает номер счёта.
* Два товара равны, если совпадает SKU или ID.

Не по всем полям.

Не по случайным значениям.

По идентификатору.

Если вы неправильно определили равенство — логика системы ломается.

---

# Шаг 6. Как красиво показать объект

Теперь вопрос:

> Как объект должен выглядеть в строковом виде?

Это `__str__`.

Это не просто “чтобы было”.

Это интерфейс.

Если я печатаю объект, я должен увидеть:

* ключевые характеристики,
* аккуратное форматирование,
* читаемость.

Пример:

```
Product(name='Laptop', price=899.99, quantity=3)
```

Или красиво:

```
Laptop — 899.99 ₽ (3 шт.)
```

Это тоже архитектурное решение.

---

# Полный пример живого разбора

Давайте разберём один пример полностью.

Берём “Интернет-магазин”.

Класс: `Product`

Атрибуты:

* name: str
* price: float
* quantity: int
* is_active: bool

Инварианты:

* price ≥ 0
* quantity ≥ 0
* name не пустой

Методы:

* restock(amount)
* sell(amount)

Равенство:

* по name (если это уникальное имя)
  или
* по id (если есть идентификатор)

Строковое представление:

* “Laptop — 899.99 ₽ (3 шт.)”

Мы только что спроектировали класс.

Кода — ноль.

Архитектуры — много.

---

# Главный вывод, который нужно проговорить

Проектирование — это ответы на вопросы.

Код — это просто реализация этих ответов.

Если вы начинаете с кода —
вы перекладываете проектирование на случай.

Если вы начинаете с модели, инвариантов и поведения —
вы пишете осмысленный код.

---

# Завершающая мысль

Перед тем как писать класс, вы должны уметь:

* назвать сущность,
* перечислить атрибуты,
* определить ограничения,
* определить поведение,
* определить равенство,
* определить формат вывода.

Если вы это сделали —
ЛР-1 у вас будет аккуратной, логичной и защищаемой.

---
Отлично. Ниже — **полный, подробный спич** именно для части:

> **Архитектура уровня ЛР-1: “слои” даже для одного класса**

Текст можно читать практически дословно на паре.
С примерами, акцентами и объяснениями.

---

# Часть занятия: Архитектура ЛР-1 — слои даже для одного класса

---

Ребята, сейчас будет очень важный момент.

Многие из вас думают:

> “Это же всего один класс. Какая тут архитектура?”

И вот здесь начинается разница между просто кодом и инженерным мышлением.

Даже один класс можно сделать:

* грязно
* или чисто

И отличие — не в количестве строк, а в разделении ответственности.

---

# Представьте две ситуации

## Ситуация 1 — “всё в одном файле”

У вас есть файл:

```
lab01.py
```

И в нём:

* объявление класса
* проверки
* print
* тестирование
* запуск
* обработка ошибок
* всё вперемешку

Это работает.
Но это не архитектура.

Это набор инструкций.

---

## Ситуация 2 — разделение на слои

У вас есть:

```
model.py
demo.py
```

И у каждого файла — своя задача.

Вот здесь появляется архитектура.

---

# Что такое слои?

Нарисуйте на доске “слоёный пирог”.

Три слоя:

1. Domain / Model
2. Application
3. Infrastructure

---

## Слой 1 — Domain / Model

Это сердце системы.

Это сам класс.

Он:

* хранит данные
* проверяет инварианты
* реализует бизнес-методы
* управляет своим состоянием

Очень важная формулировка:

> Модель знает правила предметной области.

Пример:

Если это BankAccount, модель знает:

* баланс не может быть отрицательным
* нельзя снять больше, чем есть
* счёт может быть активным или закрытым

Но модель не знает:

* как её выводят в консоль
* как пользователь вводит данные
* как запускается программа

Она знает только логику.

---

## Слой 2 — Application

Это сценарии использования.

В нашей ЛР-1 это `demo.py`.

Он:

* создаёт объекты
* вызывает методы
* демонстрирует ошибки
* показывает примеры

Очень важно:

`demo.py` не содержит бизнес-логики.

Он не проверяет баланс.
Он не решает, можно ли снять деньги.

Он просто вызывает методы модели.

---

## Слой 3 — Infrastructure

В больших проектах это:

* ввод/вывод
* базы данных
* сети
* файлы
* форматирование

В ЛР-1 инфраструктуры почти нет.

Но важно понимать:

Если бы у нас был ввод с клавиатуры или работа с файлом —
это был бы отдельный слой.

---

# Теперь ключевая мысль

Даже в маленькой ЛР-1 мы уже можем мыслить слоями.

И вот главное правило:

> Модель не должна зависеть от сценария.

То есть:

`model.py` не должен импортировать `demo.py`.

Никогда.

Если вы делаете так — вы ломаете архитектуру.

---

# Давайте разберём на примере

Представим, что мы реализуем `Product`.

---

## Плохой вариант

Внутри класса:

* методы
* print
* обработка ввода
* try/except
* тестовые вызовы

Это смешение слоёв.

Модель начинает зависеть от вывода.

---

## Хороший вариант

`model.py`:

* class Product
* инварианты
* методы restock, sell
* проверки

`demo.py`:

* создаёт Product
* вызывает sell
* печатает результат
* ловит ValueError

Теперь ответственность разделена.

---

# Почему это важно?

Потому что в больших системах:

* модель может использоваться в веб-сервере
* в API
* в тестах
* в другом приложении

Если модель зависит от консоли — она становится непереносимой.

---

# Мини-правило для ЛР-1

Проговорите чётко:

* `model.py` — умный объект
* `demo.py` — сценарии использования

Умный объект:

* знает свои данные
* знает свои ограничения
* знает свою логику

Сценарий:

* создаёт
* изменяет
* демонстрирует

---

# Очень важный момент

Кто отвечает за корректность данных?

Не demo.py.

Не пользователь.

Сам объект.

Если объект допускает отрицательный баланс — это плохая модель.

Если demo.py проверяет баланс вместо модели — это плохая архитектура.

---

# Теперь про зависимость слоёв

Нарисуйте стрелки:

```
demo → model
```

Но не:

```
model → demo
```

Зависимости идут только сверху вниз.

Это фундаментальный принцип.

---

# Почему нельзя наоборот?

Если `model.py` начнёт импортировать `demo.py`, получится циклическая зависимость.

Но даже не это главное.

Главное — логическая зависимость.

Логика не должна зависеть от способа использования.

---

# Как это влияет на уровень 3 / 4 / 5

---

## На уровне 3

У вас два файла:

* model.py
* demo.py

И этого достаточно.

---

## На уровне 4

Вы делаете код аккуратнее.

Добавляете:

* repr
* сеттер
* атрибут класса

Но архитектура остаётся та же.

---

## На уровне 5

Вот здесь начинается архитектурное мышление.

Вы можете добавить:

```
validators.py
```

И вынести туда проверки.

Теперь:

* model.py отвечает за логику
* validators.py отвечает за проверку входных данных
* demo.py отвечает за сценарии

Вы разделили ответственность.

---

# Главное, что нужно донести студентам

Архитектура — это не про “много файлов”.

Архитектура — это про ответ на вопрос:

> Кто за что отвечает?

Если каждый модуль делает одну понятную вещь —
это хорошая архитектура.

Если один файл делает всё —
это хаос.

---

# Финальный акцент

Даже один класс можно сделать:

* как учебный пример
* или как зачаток реального проекта

Разница — в слоях.

Если вы научитесь разделять:

* модель
* сценарии
* инфраструктуру

то дальше:

* Django
* FastAPI
* микросервисы

будут для вас понятны.

Потому что они построены на тех же принципах.

---

Если нужно, я могу дальше подготовить такой же полный спич для:

* “validators и инкапсуляция на 5”
* “проектирование состояния и state diagram”
* или “правильные импорты и зависимость модулей”

